{
  "block": { "description": "Group tasks together", "type": "list", "required": true },
  "rescue": { "description": "Tasks to run if the block fails", "type": "list", "required": false },
  "always": { "description": "Tasks that always run regardless of block success/failure", "type": "list", "required": false },
  "name": { "description": "Name of the block", "type": "string", "required": false },
  "when": { "description": "Conditional for the entire block", "type": "string", "required": false },
  "become": { "description": "Privilege escalation for all tasks in block", "type": "bool", "required": false },
  "become_user": { "description": "Escalation user for all tasks in block", "type": "string", "required": false },
  "tags": { "description": "Tags for the block", "type": "list", "required": false },
  "ignore_errors": { "description": "Continue on task errors within block", "type": "bool", "required": false },
  "environment": { "description": "Environment variables for block tasks", "type": "dict", "required": false },
  "vars": { "description": "Variables scoped to this block", "type": "dict", "required": false },
  "collections": { "description": "Collections to search within this block", "type": "list", "required": false }
}
