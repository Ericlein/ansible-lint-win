id = "ansible-lint-win"
name = "Ansible Lint Win"
description = "Windows-native Ansible language support — completions, hover, linting — no Python required"
version = "0.1.4"
schema_version = 1
authors = ["Eric S"]
repository = "https://github.com/Ericlein/ansible-lint-win"

[language_servers.ansible-lint-win-ls]
name = "Ansible Lint Win Language Server"
languages = ["Ansible"]

[[capabilities]]
kind = "npm:install"
package = "ansible-lint-win"
