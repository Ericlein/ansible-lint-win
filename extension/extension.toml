[package]
id = "ansible-lite"
name = "Ansible Lite"
description = "Windows-native Ansible language support — completions, hover, linting — no Python required"
version = "0.1.0"
schema_version = 1
authors = ["Eric"]
repository = "https://github.com/yourusername/ansible-ls-lite"

[language_servers.ansible-lite-ls]
name = "Ansible Lite Language Server"
languages = ["YAML"]
